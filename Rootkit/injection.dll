#include <windows.h>
#pragma comment(lib, "user32.lib")

BOOL APIENTRY DllMain(HMODULE hModule, //a handle to DLL module
                    DWORD nReason, //reason to indicate why function is called 
                    LPVOID lpReserved //reserved 
) {
    switch(nReason) 
    {
        case DLL_PROCESS_ATTACH:
            MessageBox(NULL,"Hacked by Muhammad Hani","Hacked!!!!", MB_OK);
            break;
        case DLL_PROCESS_DETACH:
            break;
        case DLL_THREAD_ATTACH:
            break;
        case DLL_THREAD_DETACH:
            break;             
    }
    return TRUE;
}
